# llm-ready
Universal npm library that makes any website LLM-ready. Auto-converts HTML pages to Markdown for AI bots, generates llms.txt, detects LLM crawlers via User-Agent and Accept headers. Drop-in Next.js middleware — install, configure 1 line, done.
